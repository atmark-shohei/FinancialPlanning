# FinancialPlanning

FIRE計画（32歳 → 40歳・世帯単位）の資産記録と戦略文書。

## ファイル

| ファイル | 役割 |
|---|---|
| `FIRE_Strategy.md` | 戦略書（全17章）。**計画の本体** |
| `Asset.md` | 資産記録。名義（👤本人／👥配偶者）と口座区分つき |
| `financialPlannnig.md` | ユーザーが貼った元データ。**編集しない** |
| `data/dividends/` | 配当実績のアーカイブ |
| `scripts/import-dividends.mjs` | 配当CSVの取り込み・重複排除・計画線との突合 |

## 配当CSVを追加したとき

証券会社（楽天・野村・SBI）のCSVは `data/dividends/raw/<年>/<証券会社>_<日付>.csv` に置き、
**必ず次を実行する**。手で集計しない。

```bash
node scripts/import-dividends.mjs
```

重複排除と計画線との乖離判定を自動で行う。詳細は `data/dividends/README.md`。

## 数字を扱うときの原則

- **推計と実測を混ぜない。** 実測があれば実測を使い、出典（アプリ名・CSV名）を明記する
- **計画の数値を書き換えたら波及先も直す。** サマリー(第1章)・第9章・第13章・実行チェックリストは相互参照している
- **訂正は履歴に残す。** 数字の根拠が変わった場合、旧説と新説の両方と理由を書く（第11.3節・第16.4節が実例）
- 金額は円単位・カンマ区切り。「万」は要約でのみ使う

## 外部送信

このリポジトリは資産残高・借入・ストックオプションを含む。**非公開GitHubリポジトリ以外に出さない。**
